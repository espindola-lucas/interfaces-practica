"use strict";
window.addEventListener('load', () => {
    const container_load = document.querySelector('.conteiner_load');
    container_load.style.opacity = 0;
    container_load.style.visiblity = 'hidden';
});